# 0.6 New note in single page app

```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: spa.js adds the note to the list and redraws it, no page reload

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa (note as JSON)
    activate server
    server-->>browser: 201 created
    deactivate server
```
